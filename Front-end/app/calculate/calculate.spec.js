'use strict';

describe('myApp.calculate module', function() {

  beforeEach(module('myApp.calculate'));

  describe('calculate controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var calculateCtrl = $controller('calculateCtrl');
      expect(calculateCtrl).toBeDefined();
    }));

  });
});